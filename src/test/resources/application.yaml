server:
  port: 8080
  servlet:
    context-path: /api

spring:
  config:
    import: optional:file:.env[.properties]
  jpa:
    hibernate:
      ddl-auto: validate

jwt:
  expiration: ${JWT_EXPIRATION}
  secret: ${JWT_SECRET}